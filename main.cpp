#include <iostream>

#include "Tester.h"


int main() {

    int length = 1000000;
    //Tester::testBubbleSort(length);
    //Tester::testSelectionSort(length);
    //Tester::testInsertionSort(length);
    //Tester::testMergeSort(length);
    //Tester::testQuickSort(length);
    Tester::testMaxHeap();

    return 0;
}
